import { FC } from 'react';
import Form from '../../components/Form';

const Login: FC = () => {
    return (
        <Form type="login" title="Log in to your account" textButton="Log In" onClick={() => {}} />
    );
};

export default Login;
